# Dotfiles manifest - maps repo files to system locations

dotfiles:
  .tmux.conf: ~/.tmux.conf
  .vimrc: ~/.vimrc
  .zshrc: ~/.zshrc
  .gitconfig: ~/.gitconfig
  # Add more dotfiles below as needed:
